# Optional: Vercel Blob for hosted shareable image URLs
# Leave empty to use local-only mode (download works, hosted URL not generated)
BLOB_READ_WRITE_TOKEN=

# Optional: your deployed URL for X share link
NEXT_PUBLIC_APP_URL=https://your-deployment.vercel.app